plugins {
    id 'application'
    id 'idea'
}

dependencies {
    // Spring boot

    // devtools
//    implementation 'org.springframework.boot:spring-boot-devtools'
}

bootJar {
    enabled = true
}

application {
    mainClassName("com.gpex.app.api.AppApiApplication")
}